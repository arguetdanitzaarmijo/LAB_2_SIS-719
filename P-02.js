function saludarUsuario(nombre) {
     return   `Hola, ${nombre} ¡Bienvenido a  seminario`;
}

// Ejemplo de uso
const nombreUsuario = "jose ";
const saludo = saludarUsuario(nombreUsuario);
console.log(saludo);